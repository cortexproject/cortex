<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Guides on Cortex</title><link>/docs/guides/</link><description>Recent content in Guides on Cortex</description><generator>Hugo</generator><language>en</language><atom:link href="/docs/guides/index.xml" rel="self" type="application/rss+xml"/><item><title>Running Cortex on Kubernetes</title><link>/docs/guides/running-cortex-on-kubernetes/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/running-cortex-on-kubernetes/</guid><description>&lt;p&gt;Because Cortex is designed to run multiple instances of each component
(ingester, querier, etc.), you probably want to automate the placement
and shepherding of these instances. Most users choose Kubernetes to do
this, but this is not mandatory.&lt;/p&gt;
&lt;h2 id="configuration"&gt;Configuration&lt;/h2&gt;
&lt;h3 id="resource-requests"&gt;Resource requests&lt;/h3&gt;
&lt;p&gt;If using Kubernetes, each container should specify resource requests
so that the scheduler can place them on a node with sufficient capacity.&lt;/p&gt;
&lt;p&gt;For example, an ingester might request:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt; resources:
 requests:
 cpu: 4
 memory: 10Gi
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;The specific values here should be adjusted based on your own
experiences running Cortex - they are very dependent on the rate of data
arriving and other factors such as series churn.&lt;/p&gt;</description></item><item><title>Getting started with gossiped ring</title><link>/docs/guides/getting-started-with-gossiped-ring/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/getting-started-with-gossiped-ring/</guid><description>&lt;p&gt;Cortex requires a Key-Value (KV) store to store the ring. It can use traditional KV stores like Consul or Etcd,
but it can also build its own KV store on top of the memberlist library using a gossip algorithm.&lt;/p&gt;
&lt;p&gt;This short guide shows how to start Cortex in &lt;a href="/docs/architecture/"&gt;single-binary mode&lt;/a&gt; with a memberlist-based ring.
To reduce the number of required dependencies in this guide, it will use &lt;a href="/docs/blocks-storage/"&gt;blocks storage&lt;/a&gt; with no shipping to external stores.
The storage engine and external storage configuration are not dependent on the ring configuration.&lt;/p&gt;</description></item><item><title>Configuring Notification using Cortex Alertmanager</title><link>/docs/guides/alertmanager-configuration/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/alertmanager-configuration/</guid><description>&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;Cortex Alertmanager notification setup follows mostly the syntax of Prometheus Alertmanager since it is based on the same codebase. The following is a description on how to load the configuration setup so that Alertmanager can use it for notification when an alert event happens.&lt;/p&gt;
&lt;h3 id="configuring-the-cortex-alertmanager-storage-backend"&gt;Configuring the Cortex Alertmanager storage backend&lt;/h3&gt;
&lt;p&gt;With the introduction of Cortex 1.8, the storage backend config option shifted to the new pattern &lt;a href="https://github.com/cortexproject/cortex/pull/3888"&gt;#3888&lt;/a&gt;. You can find the new configuration &lt;a href="/docs/configuration/configuration-file/#alertmanager_storage_config"&gt;here&lt;/a&gt;&lt;/p&gt;</description></item><item><title>Authentication and Authorisation</title><link>/docs/guides/auth/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/auth/</guid><description>&lt;p&gt;All Cortex components take the tenant ID from a header &lt;code&gt;X-Scope-OrgID&lt;/code&gt;
on each request. A tenant (also called &amp;ldquo;user&amp;rdquo; or &amp;ldquo;org&amp;rdquo;) is the owner of
a set of series written to and queried from Cortex. All Cortex components
trust this value completely: if you need to protect your Cortex installation
from accidental or malicious calls, then you must add an additional layer
of protection.&lt;/p&gt;
&lt;p&gt;Typically, this means you run Cortex behind a reverse proxy, and you must
ensure that all callers, both machines sending data over the &lt;code&gt;remote_write&lt;/code&gt;
interface and humans sending queries from GUIs, supply credentials
which identify them and confirm they are authorised. When configuring the
&lt;code&gt;remote_write&lt;/code&gt; API in Prometheus, the user and password fields of HTTP Basic
auth, or Bearer token, can be used to convey the tenant ID and/or credentials.
See the &lt;a href="/docs/guides/auth/#cortex-tenant"&gt;Cortex-Tenant&lt;/a&gt; section below for one way to solve this.&lt;/p&gt;</description></item><item><title>Capacity Planning</title><link>/docs/guides/capacity-planning/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/capacity-planning/</guid><description>&lt;p&gt;&lt;em&gt;This doc is likely out of date. It should be updated for blocks storage.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;You will want to estimate how many nodes are required, how many of
each component to run, and how much storage space will be required.
In practice, these will vary greatly depending on the metrics being
sent to Cortex.&lt;/p&gt;
&lt;p&gt;Some key parameters are:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The number of active series. If you have Prometheus already, you
can query &lt;code&gt;prometheus_tsdb_head_series&lt;/code&gt; to see this number.&lt;/li&gt;
&lt;li&gt;Sampling rate, e.g. a new sample for each series every minute
(the default Prometheus &lt;a href="https://prometheus.io/docs/prometheus/latest/configuration/configuration/"&gt;scrape_interval&lt;/a&gt;).
Multiply this by the number of active series to get the
total rate at which samples will arrive at Cortex.&lt;/li&gt;
&lt;li&gt;The rate at which series are added and removed. This can be very
high if you monitor objects that come and go - for example, if you run
thousands of batch jobs lasting a minute or so and capture metrics
with a unique ID for each one. &lt;a href="https://www.robustperception.io/using-tsdb-analyze-to-investigate-churn-and-cardinality"&gt;Read how to analyse this on
Prometheus&lt;/a&gt;.&lt;/li&gt;
&lt;li&gt;How compressible the time-series data are. If a metric stays at
the same value constantly, then Cortex can compress it very well, so
12 hours of data sampled every 15 seconds would be around 2KB. On
the other hand, if the value jumps around a lot, it might take 10KB.
There are not currently any tools available to analyse this.&lt;/li&gt;
&lt;li&gt;How long you want to retain data for, e.g. 1 month or 2 years.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Other parameters which can become important if you have particularly
high values:&lt;/p&gt;</description></item><item><title>Config for horizontally scaling the Ruler</title><link>/docs/guides/ruler-sharding/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/ruler-sharding/</guid><description>&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;One option to scale the ruler is by scaling it horizontally. However, with multiple ruler instances running, they will need to coordinate to determine which instance will evaluate which rule. Similar to the ingesters, the rulers establish a hash ring to divide up the responsibilities of evaluating rules.&lt;/p&gt;
&lt;h2 id="config"&gt;Config&lt;/h2&gt;
&lt;p&gt;In order to enable sharding in the ruler, the following flag needs to be set:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt; -ruler.enable-sharding=true
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;In addition, the ruler requires its own ring to be configured, for instance:&lt;/p&gt;</description></item><item><title>Config for sending HA Pairs data to Cortex</title><link>/docs/guides/ha-pair-handling/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/ha-pair-handling/</guid><description>&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;You can have more than a single Prometheus instance monitoring and ingesting the same metrics for redundancy. Cortex already does replication for redundancy, and it doesn&amp;rsquo;t make sense to ingest the same data twice. So in Cortex, we made sure we can dedupe the data we receive from HA Pairs of Prometheus. We do this via the following:&lt;/p&gt;
&lt;p&gt;Assume that there are two teams, each running their own Prometheus, monitoring different services. Let&amp;rsquo;s call the Prometheus instances T1 and T2. Now, if the teams are running HA pairs, let&amp;rsquo;s call the individual Prometheus instances T1.a, T1.b, and T2.a and T2.b.&lt;/p&gt;</description></item><item><title>Encryption at Rest</title><link>/docs/guides/encryption-at-rest/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/encryption-at-rest/</guid><description>&lt;p&gt;Cortex supports data encryption at rest for some storage backends.&lt;/p&gt;
&lt;h2 id="s3"&gt;S3&lt;/h2&gt;
&lt;p&gt;The Cortex S3 client supports the following server-side encryption (SSE) modes:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/dev/UsingServerSideEncryption.html"&gt;SSE-S3&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://docs.aws.amazon.com/AmazonS3/latest/dev/UsingKMSEncryption.html"&gt;SSE-KMS&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="blocks-storage"&gt;Blocks storage&lt;/h3&gt;
&lt;p&gt;The &lt;a href="/docs/blocks-storage/"&gt;blocks storage&lt;/a&gt; S3 server-side encryption can be configured as follows.&lt;/p&gt;
&lt;h3 id="s3_sse_config"&gt;&lt;code&gt;s3_sse_config&lt;/code&gt;&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;s3_sse_config&lt;/code&gt; configures the S3 server-side encryption.&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"&gt;&lt;code class="language-yaml" data-lang="yaml"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;&lt;span style="color:#f92672"&gt;sse&lt;/span&gt;:
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#75715e"&gt;# Enable AWS Server Side Encryption. Supported values: SSE-KMS, SSE-S3.&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#75715e"&gt;# CLI flag: -&amp;lt;prefix&amp;gt;.s3.sse.type&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#f92672"&gt;[type&lt;/span&gt;: &lt;span style="color:#ae81ff"&gt;&amp;lt;string&amp;gt; | default = &amp;#34;&amp;#34;]&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#75715e"&gt;# KMS Key ID used to encrypt objects in S3&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#75715e"&gt;# CLI flag: -&amp;lt;prefix&amp;gt;.s3.sse.kms-key-id&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#f92672"&gt;[kms_key_id&lt;/span&gt;: &lt;span style="color:#ae81ff"&gt;&amp;lt;string&amp;gt; | default = &amp;#34;&amp;#34;]&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#75715e"&gt;# KMS Encryption Context used for object encryption. It expects JSON formatted&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#75715e"&gt;# string.&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#75715e"&gt;# CLI flag: -&amp;lt;prefix&amp;gt;.s3.sse.kms-encryption-context&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; &lt;span style="color:#f92672"&gt;[kms_encryption_context&lt;/span&gt;: &lt;span style="color:#ae81ff"&gt;&amp;lt;string&amp;gt; | default = &amp;#34;&amp;#34;]&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h3 id="ruler"&gt;Ruler&lt;/h3&gt;
&lt;p&gt;The ruler S3 server-side encryption can be configured similarly to the blocks storage. The per-tenant overrides are supported when using the storage backend configurable with the &lt;code&gt;-ruler-storage.&lt;/code&gt; flag prefix (or their respective YAML config options).&lt;/p&gt;</description></item><item><title>Ingesters rolling updates</title><link>/docs/guides/ingesters-rolling-updates/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/ingesters-rolling-updates/</guid><description>&lt;p&gt;Cortex &lt;a href="/docs/architecture/#ingester"&gt;ingesters&lt;/a&gt; are semi-stateful.
A running ingester holds several hours of time series data in memory before they&amp;rsquo;re flushed to the long-term storage.
When an ingester shuts down because of a rolling update or maintenance, the in-memory data must not be discarded in order to avoid any data loss.&lt;/p&gt;
&lt;p&gt;The Cortex &lt;a href="/docs/blocks-storage/"&gt;blocks storage&lt;/a&gt; requires ingesters to run with a persistent disk where the TSDB WAL and blocks are stored (eg. a StatefulSet when deployed on Kubernetes).&lt;/p&gt;</description></item><item><title>Ingesters scaling up and down</title><link>/docs/guides/ingesters-scaling-up-and-down/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/ingesters-scaling-up-and-down/</guid><description>&lt;p&gt;This guide explains how to scale up and down ingesters.&lt;/p&gt;
&lt;p&gt;If you&amp;rsquo;re looking for how to run ingesters rolling updates, please refer to the &lt;a href="/docs/guides/ingesters-rolling-updates/"&gt;dedicated guide&lt;/a&gt;._&lt;/p&gt;
&lt;h2 id="scaling-up"&gt;Scaling up&lt;/h2&gt;
&lt;p&gt;Adding more ingesters to a Cortex cluster is considered a safe operation. When a new ingester starts, it will register to the &lt;a href="/docs/architecture/#the-hash-ring"&gt;hash ring&lt;/a&gt; and the distributors will reshard received series accordingly.
Ingesters that were previously receiving those series will see data stop arriving and will consider those series &amp;ldquo;idle&amp;rdquo;.&lt;/p&gt;</description></item><item><title>Migration KV Store to Memberlist</title><link>/docs/guides/migration-kv-store-to-memberlist/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/migration-kv-store-to-memberlist/</guid><description>&lt;p&gt;This guide explains how to perform a live migration of the Cortex Key-Value (KV) Store from an external backend (such as Consul or Etcd) to Memberlist.&lt;/p&gt;
&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;When deployed as microservices, Cortex relies on a KV Store to discover other Cortex components and manage ring topologies.&lt;/p&gt;
&lt;p&gt;To reduce external dependencies and simplify operations, you may want to migrate your existing KV store to Memberlist, a built-in gossip protocol implementation.&lt;/p&gt;
&lt;h2 id="step-by-step-migration-process"&gt;Step-by-step Migration Process&lt;/h2&gt;
&lt;p&gt;We can leverage the &lt;a href="https://cortexmetrics.io/docs/configuration/arguments/#multi-kv"&gt;multi KV Store&lt;/a&gt; feature to achieve a live migration.&lt;/p&gt;</description></item><item><title>Native Histograms</title><link>/docs/guides/native-histograms/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/native-histograms/</guid><description>&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;The Prometheus introduces a native histogram, a new sample type, to address several problems that the classic histograms (originally implemented histograms) have.
Please read to &lt;a href="https://prometheus.io/docs/specs/native_histograms/"&gt;Prometheus Native Histograms Document&lt;/a&gt; if more detailed information is necessary.&lt;/p&gt;
&lt;p&gt;This guide explains how to configure the native histograms on the Cortex.&lt;/p&gt;
&lt;h2 id="how-to-configure-native-histograms"&gt;How to configure native histograms&lt;/h2&gt;
&lt;p&gt;This section explains how to configure native histograms on the Cortex.&lt;/p&gt;
&lt;h3 id="enable-ingestion"&gt;Enable Ingestion&lt;/h3&gt;
&lt;p&gt;To ingest native histogram ingestion, set the flag &lt;code&gt;-blocks-storage.tsdb.enable-native-histograms&lt;/code&gt;.&lt;/p&gt;</description></item><item><title>Overrides Exporter</title><link>/docs/guides/overrides-exporter/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/overrides-exporter/</guid><description>&lt;p&gt;Since Cortex is a multi-tenant system, it supports applying limits to each tenant to prevent
any single one from using too many resources. In order to help operators understand how close
to their limits tenants are, the &lt;code&gt;overrides-exporter&lt;/code&gt; module can expose limits as Prometheus metrics.&lt;/p&gt;
&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;To update configuration without restarting, Cortex allows operators to supply a &lt;code&gt;runtime_config&lt;/code&gt;
file that will be periodically reloaded. This file can be specified under the &lt;code&gt;runtime_config&lt;/code&gt; section
of the main &lt;a href="/docs/configuration/arguments/#runtime-configuration-file"&gt;configuration file&lt;/a&gt; or using the &lt;code&gt;-runtime-config.file&lt;/code&gt;
command-line flag. This file is used to apply tenant-specific limits.&lt;/p&gt;</description></item><item><title>Use Partition Compaction in Cortex</title><link>/docs/guides/partition-compaction/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/partition-compaction/</guid><description>&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;Compactor is bounded by maximum 64GB of index file size. If compaction failed due to exceeding index file size limit, partition compaction can be enabled to allow compactor compacting into multiple blocks that have index file size stays within limit.&lt;/p&gt;
&lt;h2 id="enable-partition-compaction"&gt;Enable Partition Compaction&lt;/h2&gt;
&lt;p&gt;In order to enable partition compaction, the following flag needs to be set:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;-compactor.sharding-enabled=true # Enable sharding tenants across multiple compactor instances. This is required to enable partition compaction
-compactor.sharding-strategy=shuffle-sharding # Use Shuffle Sharding as sharding strategy. This is required to enable partition compaction
-compactor.compaction-strategy=partitioning # Use Partition Compaction as compaction strategy. To turn if off, set it to `default`
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id="migration"&gt;Migration&lt;/h3&gt;
&lt;p&gt;There is no special migration process needed to enable partition compaction. End user could enable it by setting the above configurations all at once.&lt;/p&gt;</description></item><item><title>Rate Limiting</title><link>/docs/guides/rate-limiting/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/rate-limiting/</guid><description>&lt;p&gt;Cortex applies rate limits on the write path to protect itself from overload and to enforce a fair share of capacity across tenants. This page describes the limiters that gate sample ingestion, how to configure them, and how to interpret the resulting errors and metrics.&lt;/p&gt;
&lt;h2 id="overview"&gt;Overview&lt;/h2&gt;
&lt;p&gt;There are two layers of rate limiting on the write path:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Per-tenant ingestion limit (distributor)&lt;/strong&gt; — a token-bucket limiter applied per tenant in every distributor. This is the main knob most operators tune. It is configured with &lt;code&gt;ingestion_rate&lt;/code&gt; and &lt;code&gt;ingestion_burst_size&lt;/code&gt; (and the parallel pair for native histograms).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Per-instance ingestion limit (distributor and ingester)&lt;/strong&gt; — a per-process safety valve on the total sample rate a single distributor or ingester will accept, regardless of tenant. Configured with &lt;code&gt;-distributor.instance-limits.max-ingestion-rate&lt;/code&gt; and &lt;code&gt;-ingester.instance-limits.max-ingestion-rate&lt;/code&gt;.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The per-tenant limiter rejects with HTTP &lt;code&gt;429 Too Many Requests&lt;/code&gt;; the per-instance limit rejects with HTTP &lt;code&gt;503 Service Unavailable&lt;/code&gt;. Prometheus &lt;code&gt;remote_write&lt;/code&gt; retries both by default (it retries on 5xx, and on 429 when &lt;code&gt;retry_on_http_429&lt;/code&gt; is set).&lt;/p&gt;</description></item><item><title>Rule evaluations via query frontend</title><link>/docs/guides/rule-evalutions-via-query-frontend/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/rule-evalutions-via-query-frontend/</guid><description>&lt;p&gt;This guide explains how to configure the Ruler to evaluate rules via Query Frontends instead of the Ingester/Store Gateway, and the pros and cons of rule evaluation via Query Frontend.&lt;/p&gt;
&lt;h2 id="how-to-enable"&gt;How to enable&lt;/h2&gt;
&lt;p&gt;By default, the Ruler queries both Ingesters and Store Gateway depending on the Rule time range for evaluating rules (alerting rules or recording rules). If you have set &lt;code&gt;-ruler.frontend-address&lt;/code&gt;, then the Ruler queries the Query Frontend for evaluation rules.
The address should be the gRPC listen address in host:port format.&lt;/p&gt;</description></item><item><title>Securing communication between Cortex components with TLS</title><link>/docs/guides/tls/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/tls/</guid><description>&lt;p&gt;Cortex is a distributed system with significant traffic between its services.
To allow for secure communication, Cortex supports TLS between all its
components. This guide describes the process of setting up TLS.&lt;/p&gt;
&lt;h3 id="generation-of-certs-to-configure-tls"&gt;Generation of certs to configure TLS&lt;/h3&gt;
&lt;p&gt;The first step to securing inter-service communication in Cortex with TLS is
generating certificates. A Certifying Authority (CA) will be used for this
purpose, which should be private to the organization, as any certificates signed
by this CA will have permissions to communicate with the cluster.&lt;/p&gt;</description></item><item><title>Security</title><link>/docs/guides/security/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/security/</guid><description>&lt;p&gt;Cortex must be deployed with due care over system configuration, using principles such as &amp;ldquo;least privilege&amp;rdquo; to limit any exposure due to flaws in the source code.&lt;/p&gt;
&lt;p&gt;You must configure authorisation and authentication externally to Cortex; see &lt;a href="/docs/guides/auth/"&gt;this guide&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Information about security disclosures and mailing lists is &lt;a href="https://github.com/cortexproject/cortex/blob/master/SECURITY.md"&gt;in the main repo&lt;/a&gt;&lt;/p&gt;</description></item><item><title>Shuffle Sharding</title><link>/docs/guides/shuffle-sharding/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/shuffle-sharding/</guid><description>&lt;p&gt;Cortex leverages sharding techniques to horizontally scale both single and multi-tenant clusters beyond the capacity of a single node.&lt;/p&gt;
&lt;h2 id="background"&gt;Background&lt;/h2&gt;
&lt;p&gt;The &lt;strong&gt;default sharding strategy&lt;/strong&gt; employed by Cortex distributes the workload across the entire pool of instances running a given service (eg. ingesters). For example, on the write path, each tenant&amp;rsquo;s series are sharded across all ingesters, regardless of how many active series the tenant has or how many different tenants are in the cluster.&lt;/p&gt;</description></item><item><title>Tracing</title><link>/docs/guides/tracing/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/tracing/</guid><description>&lt;p&gt;Cortex uses &lt;a href="https://www.jaegertracing.io/"&gt;Jaeger&lt;/a&gt; or &lt;a href="https://opentelemetry.io/"&gt;OpenTelemetry&lt;/a&gt; to implement distributed
tracing. We have found tracing invaluable for troubleshooting the behavior of
Cortex in production.&lt;/p&gt;
&lt;h2 id="jaeger"&gt;Jaeger&lt;/h2&gt;
&lt;h3 id="dependencies"&gt;Dependencies&lt;/h3&gt;
&lt;p&gt;In order to send traces, you will need to set up a Jaeger deployment. A
deployment includes either the Jaeger all-in-one binary or else a distributed
system of agents, collectors, and queriers. If running on Kubernetes, &lt;a href="https://github.com/jaegertracing/jaeger-kubernetes"&gt;Jaeger
Kubernetes&lt;/a&gt; is an excellent
resource.&lt;/p&gt;
&lt;h3 id="configuration"&gt;Configuration&lt;/h3&gt;
&lt;p&gt;In order to configure Cortex to send traces, you must do two things:&lt;/p&gt;</description></item><item><title>Use OpenTelemetry Collector to send metrics to Cortex</title><link>/docs/guides/use-opentelemetry-collector-to-send-metrics-to-cortex/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/use-opentelemetry-collector-to-send-metrics-to-cortex/</guid><description>&lt;p&gt;This guide explains how to configure open-telemetry collector and OTLP(OpenTelemetry Protocol) configurations in the
Cortex.&lt;/p&gt;
&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;The &lt;a href="https://opentelemetry.io/docs/collector/"&gt;open-telemetry collector&lt;/a&gt; can write collected metrics to the Cortex with
the Prometheus and OTLP formats.&lt;/p&gt;
&lt;h2 id="push-with-prometheus-format"&gt;Push with Prometheus format&lt;/h2&gt;
&lt;p&gt;To push metrics via the &lt;code&gt;Prometheus&lt;/code&gt; format, we can
use &lt;a href="https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/exporter/prometheusremotewriteexporter"&gt;prometheusremotewrite&lt;/a&gt;
exporter in the open-telemetry collector.
In the &lt;code&gt;exporters&lt;/code&gt; and &lt;code&gt;service&lt;/code&gt; sections in the open-telemetry collector yaml file, we can add as follows:&lt;/p&gt;
&lt;pre tabindex="0"&gt;&lt;code&gt;exporters:
 prometheusremotewrite:
 endpoint: http://&amp;lt;cortex-endpoint&amp;gt;/api/v1/push
 headers:
 X-Scope-OrgId: &amp;lt;orgId&amp;gt;

...

service:
 pipelines:
 metrics:
 receivers: [...]
 processors: [...]
 exporters: [prometheusremotewrite]
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Please refer to &lt;a href="/docs/guides/auth/"&gt;Authentication and Authorisation&lt;/a&gt; section for the
&lt;code&gt;X-Scope-OrgId&lt;/code&gt; explanation.&lt;/p&gt;</description></item><item><title>Zone Aware Replication</title><link>/docs/guides/zone-aware-replication/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/zone-aware-replication/</guid><description>&lt;p&gt;Cortex supports data replication for different services. By default, data is transparently replicated across the whole pool of service instances, regardless of whether these instances are all running within the same availability zone (or data center, or rack) or in different ones.&lt;/p&gt;
&lt;p&gt;It is completely possible that all the replicas for the given data are held within the same availability zone, even if the Cortex cluster spans multiple zones. Storing multiple replicas for a given data within the same availability zone poses a risk for data loss if there is an outage affecting various nodes within a zone or a full zone outage.&lt;/p&gt;</description></item><item><title>Parquet Mode</title><link>/docs/guides/parquet-mode/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/parquet-mode/</guid><description>&lt;h2 id="overview"&gt;Overview&lt;/h2&gt;
&lt;p&gt;Parquet mode in Cortex provides an experimental feature that converts TSDB blocks to Parquet format for improved query performance and storage efficiency on older data. This feature is particularly beneficial for long-term storage scenarios where data is accessed less frequently but needs to be queried efficiently.&lt;/p&gt;
&lt;p&gt;The parquet mode consists of two main components:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Parquet Converter&lt;/strong&gt;: Converts TSDB blocks to Parquet format&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Parquet Queryable&lt;/strong&gt;: Enables querying of Parquet files with fallback to TSDB blocks&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="why-parquet-mode"&gt;Why Parquet Mode?&lt;/h2&gt;
&lt;p&gt;Traditional TSDB format and Store Gateway architecture face significant challenges when dealing with long-term data storage on object storage:&lt;/p&gt;</description></item><item><title>Protecting Cortex from Heavy Queries</title><link>/docs/guides/protecting-cortex-from-heavy-queries/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/protecting-cortex-from-heavy-queries/</guid><description>&lt;p&gt;PromQL is powerful, and is able to result in query requests that have very wide range of data fetched and samples processed. Heavy queries can cause:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;CPU on any query component to be partially exhausted, increasing latency and causing incoming queries to queue up with high chance of time-out.&lt;/li&gt;
&lt;li&gt;CPU on any query component to be fully exhausted, causing GC to slow down leading to the pod being out-of-memory and killed.&lt;/li&gt;
&lt;li&gt;Heap memory on any query component to be exhausted, leading to the pod being out-of-memory and killed.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;It&amp;rsquo;s important to protect Cortex components by setting appropriate limits and throttling configurations based on your infrastructure and data ingested by the customers.&lt;/p&gt;</description></item><item><title>User Overrides API</title><link>/docs/guides/overrides/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/overrides/</guid><description>&lt;p&gt;The User Overrides API provides a RESTful interface for managing tenant-specific limit overrides at runtime without requiring manual edits to the runtime configuration file or service restarts.&lt;/p&gt;
&lt;h2 id="context"&gt;Context&lt;/h2&gt;
&lt;p&gt;Cortex is a multi-tenant system that applies resource limits to each tenant to prevent any single tenant from using too many resources. These limits can be configured globally via the &lt;code&gt;limits&lt;/code&gt; section in the main configuration file, or per-tenant via the &lt;code&gt;runtime_config&lt;/code&gt; file.&lt;/p&gt;</description></item><item><title>Supported architectures</title><link>/docs/guides/supported-architectures/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/supported-architectures/</guid><description>&lt;p&gt;Cortex release artifacts are built for the operating systems and architectures
listed below. Use these targets when selecting container images, binary
artifacts, or OS packages for production deployments.&lt;/p&gt;
&lt;h2 id="supported-release-targets"&gt;Supported release targets&lt;/h2&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;Artifact type&lt;/th&gt;
					&lt;th&gt;Operating system&lt;/th&gt;
					&lt;th&gt;Architectures&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;Container images&lt;/td&gt;
					&lt;td&gt;Linux&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;amd64&lt;/code&gt;, &lt;code&gt;arm64&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Cortex binary&lt;/td&gt;
					&lt;td&gt;Linux&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;amd64&lt;/code&gt;, &lt;code&gt;arm64&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Cortex binary&lt;/td&gt;
					&lt;td&gt;darwin (macOS)&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;amd64&lt;/code&gt;, &lt;code&gt;arm64&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;query-tee&lt;/code&gt; binary&lt;/td&gt;
					&lt;td&gt;Linux&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;amd64&lt;/code&gt;, &lt;code&gt;arm64&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;&lt;code&gt;query-tee&lt;/code&gt; binary&lt;/td&gt;
					&lt;td&gt;darwin (macOS)&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;amd64&lt;/code&gt;, &lt;code&gt;arm64&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;Debian package&lt;/td&gt;
					&lt;td&gt;Linux&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;amd64&lt;/code&gt;, &lt;code&gt;arm64&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;RPM package&lt;/td&gt;
					&lt;td&gt;Linux&lt;/td&gt;
					&lt;td&gt;&lt;code&gt;amd64&lt;/code&gt;, &lt;code&gt;arm64&lt;/code&gt;&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The CI and release pipelines build Cortex with &lt;code&gt;GOOS&lt;/code&gt; and &lt;code&gt;GOARCH&lt;/code&gt; targets
matching these rows. Automated tests run against Linux &lt;code&gt;amd64&lt;/code&gt; and &lt;code&gt;arm64&lt;/code&gt;
Cortex images.&lt;/p&gt;</description></item><item><title>Tenant ID Naming Conventions</title><link>/docs/guides/tenant-id-naming-conventions/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/tenant-id-naming-conventions/</guid><description>&lt;h2 id="tenant-id-naming"&gt;Tenant ID naming&lt;/h2&gt;
&lt;p&gt;The tenant ID (also called &amp;ldquo;user ID&amp;rdquo; or &amp;ldquo;org ID&amp;rdquo;) is the unique identifier of a tenant within a Cortex cluster. The tenant ID is opaque information to Cortex, which doesn&amp;rsquo;t make any assumptions on its format/content, but its naming has three limitations:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Supported characters&lt;/li&gt;
&lt;li&gt;Invalid tenant IDs&lt;/li&gt;
&lt;li&gt;Length&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id="supported-characters"&gt;Supported characters&lt;/h3&gt;
&lt;p&gt;The following character sets are generally &lt;strong&gt;safe for use in the tenant ID&lt;/strong&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Alphanumeric characters
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;0-9&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a-z&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;A-Z&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Special characters
&lt;ul&gt;
&lt;li&gt;Exclamation point (&lt;code&gt;!&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Hyphen (&lt;code&gt;-&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Underscore (&lt;code&gt;_&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Single Period (&lt;code&gt;.&lt;/code&gt;), but the tenant IDs &lt;code&gt;.&lt;/code&gt; and &lt;code&gt;..&lt;/code&gt; are considered invalid&lt;/li&gt;
&lt;li&gt;Asterisk (&lt;code&gt;*&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Single quote (&lt;code&gt;'&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Open parenthesis (&lt;code&gt;(&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;Close parenthesis (&lt;code&gt;)&lt;/code&gt;)&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;All other characters are not safe to use. In particular, slashes &lt;code&gt;/&lt;/code&gt; and whitespaces (&lt;code&gt; &lt;/code&gt;) are &lt;strong&gt;not supported&lt;/strong&gt;.&lt;/p&gt;</description></item><item><title>Glossary</title><link>/docs/guides/glossary/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/glossary/</guid><description>&lt;h3 id="active-series"&gt;Active series&lt;/h3&gt;
&lt;p&gt;An active series is a &lt;a href="/docs/guides/glossary/#series"&gt;series&lt;/a&gt; held in an ingester that has received at least one &lt;a href="/docs/guides/glossary/#sample"&gt;sample&lt;/a&gt; within the active-series idle timeout window (configured via &lt;code&gt;-ingester.active-series-metrics-idle-timeout&lt;/code&gt;, default &lt;code&gt;10m&lt;/code&gt;). Active series are a subset of &lt;a href="/docs/guides/glossary/#memory-series"&gt;memory series&lt;/a&gt;: once a series stops receiving samples for longer than the idle timeout, it is no longer counted as active, even if it remains in the ingester&amp;rsquo;s memory until the TSDB head garbage-collects it.&lt;/p&gt;
&lt;p&gt;Active series are tracked per-tenant and exposed via the &lt;code&gt;cortex_ingester_active_series&lt;/code&gt; metric. The &lt;code&gt;max_global_series_per_user&lt;/code&gt; and &lt;code&gt;max_global_series_per_metric&lt;/code&gt; limits are enforced against this count. The feature is enabled by default and controlled by &lt;code&gt;-ingester.active-series-metrics-enabled&lt;/code&gt;.&lt;/p&gt;</description></item><item><title/><link>/docs/guides/timeout-classification/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>/docs/guides/timeout-classification/</guid><description>&lt;h1 id="timeout-classification"&gt;Timeout Classification&lt;/h1&gt;
&lt;p&gt;Timeout classification lets Cortex distinguish between query timeouts caused by expensive user queries (4XX) and those caused by system issues (5XX). When enabled, queries that spend most of their time in PromQL evaluation are returned as &lt;code&gt;422 Unprocessable Entity&lt;/code&gt; instead of &lt;code&gt;503 Service Unavailable&lt;/code&gt;, giving callers a clear signal to simplify the query rather than retry.&lt;/p&gt;
&lt;h2 id="how-it-works"&gt;How It Works&lt;/h2&gt;
&lt;p&gt;When a query (instant/ranged, other apis are unchanged) arrives at the querier, the feature:&lt;/p&gt;</description></item></channel></rss>